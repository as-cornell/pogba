{#
    Sets content for block--as-events.html.twig which pulls in event-list.twig.
    twig tweak
 #}
 {% set eventsLabelValue = content.name[0]['#context']['value'] %}
 {% set eventsShownValue = content.field_events_shown[0]['#markup'] %}
 {% set eventsKeywordsValue = content.field_keyword_params[0]['#context']['value'] %}

{# {{kint(_context)}} #}

    {{ drupal_block('events_block', {label: eventsLabelValue, events_shown: eventsShownValue , keyword_params: eventsKeywordsValue}) }}
